procedure OneInt is
   A : Integer;
   B : Integer;
   C : Boolean;
   F : Float;
   E : Boolean;

  function getInt(AB:Integer) return Boolean is
	Begin
	end getInt;

  D : Boolean;
BEGIN
  if C and (D or E) then
      A:= B + 2;
	end if;
  C := getInt(A);
	
  A := 5 + (2 + 25) - 8 * B;
  --B:= A * retFloat();
  for A:=1 to 10
    B:= 3;
  end for;
END OneInt;




-- Result of execution

-- The value of Index is         23
-- The value of Index is      35
