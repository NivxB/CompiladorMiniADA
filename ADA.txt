procedure OneInt is
   Index : Integer ;
begin
   if Index then
      Index := SUM_NUM(3) * 24;
  end if;
  TestString := true;
  Put("The value of Index is");
   --Put(Index);              -- The default field width is 11 columns
   New_Line;
   Index := Index + 12;
   Put("The value of Index is");
   Put(Index, 8);
   New_Line;
   for cu := SUM_NUM(3) to 25
        Put("HOLA");
   end for;
end OneInt;




-- Result of execution

-- The value of Index is         23
-- The value of Index is      35
