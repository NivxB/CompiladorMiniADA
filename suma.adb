procedure pruebaSuma is
   	a,b,c: Integer;
begin
   	--test := 0;
   	b:= 2 + 3;
   	a:= (b - 3 * 2) / 3 - 5;
   	c:= 4;
   	if(a>c or b>c and a=0 or a<c) then
   		c:=8;
   	end if;
   	-- a:= 3;
end pruebaSuma;
